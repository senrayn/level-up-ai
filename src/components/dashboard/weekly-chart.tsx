"use client"
import { GlassCard } from "@/components/ui/glass-card"; import { mockWeeklyData } from "@/lib/mock-data"; import { BarChart3 } from "lucide-react"; import { motion } from "framer-motion"
export function WeeklyChart(){const mx=Math.max(...mockWeeklyData.map(d=>d.xp),1);return <GlassCard>
  <div className="flex items-center gap-2 mb-4"><BarChart3 className="h-4 w-4 text-slate-500"/><h3 className="text-sm font-semibold text-text">Weekly Progress</h3></div>
  <div className="flex items-end justify-between gap-1 h-28">{mockWeeklyData.map((d,i)=>{const h=(d.xp/mx)*100;const t=i===5;return <div key={d.day} className="flex flex-1 flex-col items-center gap-1.5"><div className="relative flex w-full flex-col justify-end" style={{height:90}}><motion.div initial={{height:0}} animate={{height:`${Math.max(h,4)}%`}} transition={{duration:.5,delay:i*.05}} className={`w-full rounded-t-lg ${t?"bg-gradient-to-t from-primary to-mint":"bg-slate-300"}`}/></div><span className={`text-[10px] font-medium ${t?"text-primary":"text-slate-500"}`}>{d.day}</span></div>})}</div>
  <div className="mt-3 flex items-center justify-between text-[11px] text-slate-500"><span>{mockWeeklyData.reduce((a,b)=>a+b.tasks,0)} tasks</span><span>{mockWeeklyData.reduce((a,b)=>a+b.xp,0)} XP</span></div></GlassCard>}
